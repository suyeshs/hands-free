{
  "name": "tenant-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "validate": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "npm run validate && wrangler deploy",
    "deploy:single": "bash ../../../../deploy-tenant-worker.sh",
    "deploy:all": "bash ../../../scripts/deploy-all-tenant-workers.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.0.0"
  }
}

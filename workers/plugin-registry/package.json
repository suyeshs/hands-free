{
  "name": "handsfree-plugin-registry",
  "version": "1.0.0",
  "description": "Cloudflare Worker for HandsFree Plugin Registry",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "upload-plugins": "bun run scripts/upload-plugins.ts"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240925.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0",
    "typescript": "^5.6.3"
  }
}
